# ai-benchmark-platform 已实现功能记录

> 更新时间：2026-04-09  
> 说明：记录当前已落地能力，覆盖任务配置生成、数据集同步/转换、前端预览链路，便于后续接入真实后端执行服务。

## 1. 当前整体状态

项目已从“纯 mock 展示”升级为“可创建任务、生成配置、管理真实本地数据集”的状态，主线覆盖：

- 大模型评测（LLM）
- 加速卡评测（Accelerator）
- 数据集目录同步与前端真实预览

---

## 2. 任务配置链路（LLM / Accelerator）

### 2.1 LLM：OpenCompass 风格配置生成

- 配置构建入口：`src/utils/opencompassConfig.ts`
- 新建任务入口：`src/components/Task/LLMEvaluationConfig.tsx`
- 重测入口：`src/components/TestResult/RetestModal.tsx`
- 生成结构：
  - `models`
  - `datasets`
  - `infer`
  - `eval`
  - `summarizer`

### 2.2 Accelerator：统一评测配置生成

- 配置构建入口：`src/utils/acceleratorBenchmarkConfig.ts`
- 任务入口：`src/components/Task/AcceleratorEvaluationConfig.tsx`
- 生成结构：
  - `accelerator`
  - `workloads`
  - `offline_checks`
  - `execution`
  - `summarizer`

### 2.3 配置落盘与任务对象回填

- 前端服务：`src/services/configFileService.ts`
- 本地 API（Vite middleware）：`vite.config.ts`
  - `POST /api/v1/config-files`
  - 输出目录：
    - `generated-configs/llm/`
    - `generated-configs/accelerator/`
- 类型扩展：`src/types/index.ts`
  - `llmConfig.openCompassConfig`
  - `acceleratorConfig.acceleratorBenchmarkConfig`
  - `generatedConfigFile`
  - `configGenerationError`

### 2.4 前端可见性

- 任务详情页展示配置文件路径与配置状态：`src/pages/TaskCockpit.tsx`

---

## 3. 数据集同步与本地产物

### 3.1 数据集同步脚本（全量优先）

- 脚本：`scripts/build_dataset_catalog.py`
- 命令：
  - `npm run sync:datasets`（全量优先）
  - `npm run sync:datasets:preview`（仅预览模式）
- 下载策略（开源数据集）：
  1. HuggingFace datasets-server parquet 接口
  2. `datasets` 库 fallback（`save_to_disk` 产出 `raw/hf`）
  3. GitHub 原始文件 fallback
  4. 最后 fallback sample（避免前端空白）
- 支持 token：`HF_TOKEN` / `HUGGINGFACE_TOKEN`
- HuggingFace 缓存目录固定到项目内 `.cache/huggingface`

### 3.2 已落地产物

- `public/datasets/catalog.json`
- `public/datasets/open-source/<id>/sample.jsonl`
- `public/datasets/open-source/<id>/raw/parquet/*`（可用时）
- `public/datasets/open-source/<id>/raw/hf/*`（datasets fallback）
- `public/datasets/open-source/<id>/raw/source/*`（github fallback）
- `public/datasets/self-built/<id>/sample.jsonl`

### 3.3 ImageNet 特殊处理

- `imagenet` 使用 `ILSVRC/imagenet-1k`
- 支持“单个 parquet 分片”下载路径（避免一次拉完整快照）
- 若 token 未开启 gated 权限会出现 403（需在 HF token 权限中放开 public gated repo 访问）

---

## 4. Parquet/Arrow 转换为 test.jsonl

### 4.1 转换脚本

- 脚本：`scripts/parquet_to_test_jsonl.py`
- 命令：`npm run parquet:to-test-jsonl`

### 4.2 支持输入

- `raw/parquet/*.parquet`
- `raw/hf/`（HuggingFace `save_to_disk`，底层 arrow）

### 4.3 输出规则

- 每个数据集目录输出：`<dataset>/test.jsonl`
- 若包含图片字段（bytes 或 path）：
  - 写入 `<dataset>/image/`
  - 在 `test.jsonl` 中存相对路径（如 `image/image_00000001.jpg`）

---

## 5. 前端数据集页面改造（查看详情链路）

### 5.1 预览数据加载优先级

- 服务：`src/services/datasetCatalogService.ts`
- 预览加载顺序：
  1. `test.jsonl`
  2. `sample.jsonl`
  3. `catalog.json` 的 `previewData` 兜底

### 5.2 样本数展示逻辑

- `sampleCount` 优先取 `test.jsonl` 实际行数（失败再回退 `sample.jsonl`，再回退 catalog 原值）

### 5.3 已接入页面

- 数据集管理页：`src/pages/DatasetManagement/index.tsx`
- 数据集详情弹窗：`src/pages/DatasetManagement/DatasetDetailDashboard.tsx`
- 资产管理页预览弹窗：`src/pages/Assets/Datasets/DataManager.tsx`

### 5.4 交互细节

- 查看数据集时，预览表格最多展示前 100 条
- 预览表格支持图片缩略图（基于相对路径）
- 页面显示“预览来源”（`test.jsonl` / `sample.jsonl` / `catalog previewData`）
- 已移除“样本数量不足1000条”提示与高亮

---

## 6. 当前可执行命令

```bash
# 1) 安装依赖
npm install

# 2) 同步数据集（全量优先）
npm run sync:datasets

# 3) 仅同步预览样本
npm run sync:datasets:preview

# 4) 将 parquet/hf-arrow 转为 test.jsonl
npm run parquet:to-test-jsonl

# 5) 启动前端
npm run dev
```

---

## 7. 已知限制

1. 配置文件写入目前仍是 Vite 本地中间件方案（开发期），未接真实后端服务。
2. 部分 gated 数据集（典型是 ImageNet）依赖 HF 账号权限与 token 配置，可能失败。
3. 任务执行、推理、评测、报告回传主链路尚未接入（当前重点是“建任务 + 配置落盘 + 数据集可视化”）。

---

## 8. 下一步建议（按优先级）

1. 接入真实后端任务服务（提交 / 状态 / 结果回传）。
2. 将配置落盘 API 从 Vite middleware 迁移到后端。
3. 建立数据集元信息维护流程（`sampleCount`、来源、更新时间）并支持一键校验。
4. 打通自建数据集上传（CSV/JSONL/Parquet）后自动生成 `test.jsonl` + 更新 catalog。
5. 统一评测结果产物结构（raw results + summary）并绑定前端展示。

